// Sum of first n Odd numbers :

#include <stdio.h>
int main()
{
    int range;
    scanf("%d", &range);
    /*int sum=0,num;
    for(num=1 ; num<=2*range-1 ; num+=2)
        sum=sum + num;
    printf("%d",sum);*/
    printf("%d", range * range);
    return 0;
}
